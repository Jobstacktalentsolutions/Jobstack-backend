/** Per-file verification state for employer and jobseeker verification documents. */
export enum VerificationDocumentStatus {
  PENDING = 'PENDING',
  APPROVED = 'APPROVED',
  REJECTED = 'REJECTED',
}
